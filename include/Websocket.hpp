#ifndef WEBSOCKET_HPP
#define WEBSOCKET_HPP

#include <thread>

using namespace std;

class Websocket: public std::thread {
    private:
        /* data */
    public:
        Websocket(/* args */);
};

#endif